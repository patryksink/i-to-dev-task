{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <div>
        <h1 class="mb-5">Welcome page</h1>
        {% if is_granted('ROLE_ADMIN') %}
            <a class="btn btn-primary" href="{{ path('admin') }}">Go to admin section</a>
        {% endif %}
    </div>
{% endblock %}
